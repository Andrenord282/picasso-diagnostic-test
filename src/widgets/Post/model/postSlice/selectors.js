export const selectPostLoaded = (state) => state.post.postLoaded;
export const selectPostContent = (state) => state.post.postContent;
export const selectPostAuthor = (state) => state.post.postAuthor;
export const selectPostComents = (state) => state.post.poscComments;
